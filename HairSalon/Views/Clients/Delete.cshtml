@{
  Layout = "_Layout";
}

@model HairSalon.Models.Client

<h2>Delete all of it for @Html.DisplayFor(model => model.ClientName)? (for real for real?)</h2>

@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn" />
}
<br>
<div>
  <span>
      <span>@Html.ActionLink("Back to clients", "Index")</span>
  </span>
</div>
